## 제목

📄 [문서][API] 관리자 API Swagger 명세 보강

## 본문

## 📝 작성·수정할 문서

- 관리자 API의 Swagger 명세. 별도 문서 파일이 아니라 각 컨트롤러에 붙는 OpenAPI 설명이며, development 프로파일에서 `/docs/swagger-ui` 로 열린다.
- 대상은 develop(4199d0b) 기준으로 아직 Swagger 설명이 붙지 않은 관리자 컨트롤러 전부다. 지금은 아티스트·축제·임포트가 여기 해당하고, 인증·주최는 이미 붙어 있어 대상이 아니다.
- 임포트는 이슈 #39·#41이 엔드포인트를 더 붙이는 중이다. 그 PR들이 추가하는 엔드포인트는 각 PR에서 직접 설명을 달고, 이 이슈는 develop에 이미 있는 것만 다룬다.

## 🎯 담을 내용

- 컨트롤러마다 무엇을 다루는 묶음인지, 엔드포인트마다 무엇을 하는지 한 줄 설명
- 요청 항목과 응답 항목의 의미. 값의 범위나 형식이 정해져 있으면 그것도 함께
- 실패했을 때 어떤 에러 코드가 내려가는지
- 인증이 필요한지, 어떤 토큰을 쓰는지
- 이미 설명이 붙어 있는 인증·주최 컨트롤러를 기준으로 삼아 표현 방식을 맞춘다

## 🙋‍♂️ 담당자

- 담당: 이름
